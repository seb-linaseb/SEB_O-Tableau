{% extends 'base.html.twig' %}

{% block title %}O'Tableau{% endblock %}

{% block content %}

    <div class="backend_person">
        {% for person in person %}
            <div>
                <a href="{{ path('admin_person_show', { 'id': person.id }) }}">{{person.name}}</a>
            </div>
        {% endfor %}    
    </div>
    <a href="{{ path('admin_person_new') }}">NEW</a>
{% endblock %}
{% block footer %}
{% endblock %}